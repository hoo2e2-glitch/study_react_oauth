import { Link } from "react-router-dom";
import styled from "styled-components";

const S = {}

  S.Thumbnail = styled.img`
      height: 200px;
      width: 200px;
      margin: 0 auto;
  `

export default S;